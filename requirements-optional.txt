# Optional extras (HYG-5): playwright and websocket-client, needed only by the narrow
# Voyageur/HBCA browser-automation code paths (see requirements-optional.in for which).
# Neither is required for the base app to run - both are imported lazily and degrade
# gracefully (or raise a clear install hint) when missing. Source of truth is
# requirements-optional.in - do not hand-edit this file. Regenerate with:
#   python -P -m piptools compile --output-file=requirements-optional.txt --no-header --strip-extras requirements-optional.in
#
# Install with: pip install -r requirements.txt -r requirements-optional.txt
greenlet==3.5.5
    # via playwright
playwright==1.62.0
    # via -r requirements-optional.in
pyee==13.0.1
    # via playwright
typing-extensions==4.16.0
    # via pyee
websocket-client==1.9.0
    # via -r requirements-optional.in
